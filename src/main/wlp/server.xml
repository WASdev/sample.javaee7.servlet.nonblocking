<server>

    <featureManager>
        <feature>jsp-2.3</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

    <application name="sample.javaee7.servlet.nonblocking" context-root="/sample.javaee7.servlet.nonblocking" location="${appLocation}" type="war"/>

</server>
